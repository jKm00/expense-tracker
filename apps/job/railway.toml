[build]
buildCommand = "pnpm --filter @expense-tracker/job build"
watchPatterns = ["apps/job/src/**"]

[deploy]
startCommand = "pnpm --filter @expense-tracker/job start"
cronSchedule = "0 0 * * *"
restartPolicyType = "NEVER"
